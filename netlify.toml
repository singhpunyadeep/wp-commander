[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."claude-proxy"]
  timeout = 26

[functions."wp-proxy"]
  timeout = 26

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
